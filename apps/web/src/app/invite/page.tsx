import { InvitePage } from "@/components/projects/invite-page";

export const metadata = {
	title: "Project invitation | Kousa",
	referrer: "no-referrer" as const,
};

export default function Page() {
	return <InvitePage />;
}
